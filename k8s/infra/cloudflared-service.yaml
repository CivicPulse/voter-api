apiVersion: v1
kind: Service
metadata:
  name: cloudflared
  namespace: civpulse-infra
  labels:
    app: cloudflared
spec:
  selector:
    app: cloudflared
  ports:
    - name: metrics
      port: 2000
      targetPort: 2000
