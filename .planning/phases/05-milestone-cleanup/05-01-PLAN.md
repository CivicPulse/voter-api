---
phase: 05-milestone-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/pipeline-walkthrough.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/v1.0-MILESTONE-AUDIT.md
  - .planning/phases/03-claude-code-skills/03-CONTEXT.md
autonomous: true
requirements:
  - DEM-01

must_haves:
  truths:
    - "Walkthrough branch reference removed — clone instruction assumes main"
    - "Walkthrough election_event_id description is accurate (NULL after import, resolved by resolve-elections)"
    - "Walkthrough includes resolve-elections step with real terminal output"
    - "Walkthrough includes API verification showing non-null election_event_id"
    - "REQUIREMENTS.md DEM-01 shows Complete"
    - "ROADMAP.md Phase 5 success criteria reflects expanded scope"
    - "v1.0-MILESTONE-AUDIT.md is tracked in git"
    - "03-CONTEXT.md is tracked in git"
  artifacts:
    - path: "docs/pipeline-walkthrough.md"
      provides: "Corrected pipeline walkthrough with resolve-elections step"
      contains: "resolve-elections"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Accurate DEM-01 traceability"
      contains: "Complete"
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 5 success criteria"
      contains: "Nyquist validation"
  key_links:
    - from: "docs/pipeline-walkthrough.md"
      to: "voter-api import resolve-elections"
      via: "new walkthrough section documenting the CLI command"
      pattern: "resolve-elections"
---

<objective>
Fix documentation inaccuracies and stale traceability identified in the v1.0 milestone audit.

Purpose: The walkthrough is the final deliverable for the "Better Imports" milestone. It must accurately describe the full pipeline including the election_event_id FK resolution step, and all traceability artifacts must be accurate before milestone archival.

Output: Corrected walkthrough with resolve-elections step and real terminal output, updated REQUIREMENTS.md and ROADMAP.md, committed audit and context files.
</objective>

<execution_context>
@/home/kwhatcher/.claude/get-shit-done/workflows/execute-plan.md
@/home/kwhatcher/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-milestone-cleanup/05-CONTEXT.md
@.planning/phases/05-milestone-cleanup/05-RESEARCH.md
@docs/pipeline-walkthrough.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full walkthrough review, corrections, and resolve-elections step</name>
  <files>docs/pipeline-walkthrough.md</files>
  <action>
Read the entire docs/pipeline-walkthrough.md (all ~874 lines) and fix all inaccuracies found. Known issues to fix:

1. **Branch reference (near line 21)**: Remove the `git checkout worktree-better-imports   # or main once merged` line entirely. The Prerequisites section should just be:
   ```bash
   git clone https://github.com/CivicPulse/voter-api.git
   cd voter-api
   uv sync
   ```

2. **election_event_id claim (near line 366)**: Find the text that claims election_event_id "is resolved during import" and rewrite it to accurately explain that election_event_id is stored as NULL after import and is resolved by a separate `resolve-elections` step documented later in the walkthrough.

3. **Add resolve-elections section**: After the import step and before the API verification step, add a new section following the walkthrough's existing pattern (heading, explanatory prose, fenced bash command block with explicit env vars, expected output block). The section should:
   - Run `voter-api import resolve-elections` against local dev DB
   - Show real terminal output captured from running the command (requires docker-compose PostGIS to be running with imported demo data)
   - To capture real output: ensure docker compose is up and migrations applied, then follow the walkthrough's own import steps to load data, then run resolve-elections and capture output

4. **Add API verification**: After the resolve-elections section, add a curl command showing an election detail response with non-null election_event_id, following the walkthrough's existing curl + jq verification pattern.

5. **Full review**: Read every section for any other inaccuracies (stale references, wrong counts, incorrect descriptions) and fix them.

Commit strategy: One commit for walkthrough fixes with message like `docs(phase-05): fix walkthrough inaccuracies and add resolve-elections step`.
  </action>
  <verify>
    <automated>grep -c "resolve-elections" docs/pipeline-walkthrough.md | xargs test 1 -le && ! grep -q "git checkout worktree-better-imports" docs/pipeline-walkthrough.md && echo "PASS"</automated>
  </verify>
  <done>Walkthrough has no branch checkout instruction, election_event_id description is accurate, resolve-elections step is documented with real output, API verification shows non-null election_event_id</done>
</task>

<task type="auto">
  <name>Task 2: Update traceability files and commit planning artifacts</name>
  <files>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/v1.0-MILESTONE-AUDIT.md, .planning/phases/03-claude-code-skills/03-CONTEXT.md</files>
  <action>
1. **REQUIREMENTS.md**: Read current state. If DEM-01 row does NOT already show "Complete", update it. If it already shows "Complete" (per research findings), verify and skip the edit. Per user decision: "Just fix DEM-01 — no full re-verification needed."

2. **ROADMAP.md**: Update Phase 5 success criteria to reflect expanded scope. Current criteria mention only walkthrough and REQUIREMENTS.md fixes. Add:
   - Nyquist validation run for phases 3 and 4
   - Full walkthrough review (not just known issues)
   - Planning artifacts committed (audit file, 03-CONTEXT.md)
   Update the Plans count if needed and ensure the plan list entry for 05-01 and 05-02 are listed.

3. **v1.0-MILESTONE-AUDIT.md**: This untracked file already exists. Commit it as-is — it provides traceability for why Phase 5 changes were made. Commit message: `docs(phase-05): commit v1.0 milestone audit artifact`

4. **03-CONTEXT.md**: This untracked file in `.planning/phases/03-claude-code-skills/` already exists. Commit it as-is — it's a planning artifact from Phase 3 discussion that was never committed. Commit message: `docs(phase-03): commit phase context artifact`

Commit strategy: Separate commits per logical change as specified by user decision.
  </action>
  <verify>
    <automated>grep "DEM-01" .planning/REQUIREMENTS.md | grep -q "Complete" && grep -q "Nyquist" .planning/ROADMAP.md && git status --porcelain .planning/v1.0-MILESTONE-AUDIT.md .planning/phases/03-claude-code-skills/03-CONTEXT.md | wc -l | xargs test 0 -eq 2>/dev/null; echo "Check git log for committed artifacts"</automated>
  </verify>
  <done>REQUIREMENTS.md DEM-01 shows "Complete", ROADMAP.md Phase 5 success criteria updated with expanded scope including Nyquist validation, v1.0-MILESTONE-AUDIT.md committed, 03-CONTEXT.md committed — each as separate commits</done>
</task>

</tasks>

<verification>
- `grep -q "resolve-elections" docs/pipeline-walkthrough.md` — resolve-elections step exists
- `! grep -q "git checkout worktree-better-imports" docs/pipeline-walkthrough.md` — branch reference removed
- `grep "DEM-01" .planning/REQUIREMENTS.md | grep -q "Complete"` — traceability accurate
- `git log --oneline -5` — shows separate commits for walkthrough, REQUIREMENTS.md, audit file, 03-CONTEXT.md
- `uv run ruff check . && uv run ruff format --check .` — lint clean (no accidental code changes)
</verification>

<success_criteria>
1. Walkthrough accurately documents the full pipeline including resolve-elections step
2. No stale branch references in walkthrough
3. DEM-01 traceability is accurate
4. All untracked planning artifacts are committed
5. Lint passes (no accidental code edits)
</success_criteria>

<output>
After completion, create `.planning/phases/05-milestone-cleanup/05-01-SUMMARY.md`
</output>
