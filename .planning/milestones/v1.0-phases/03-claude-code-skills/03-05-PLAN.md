---
phase: 03-claude-code-skills
plan: 05
type: execute
wave: 4
depends_on:
  - 03-02
  - 03-03
  - 03-04
files_modified:
  - data/elections/2026-05-19/
  - data/elections/2026-03-17/
  - data/elections/2026-03-10/
  - data/candidates/
autonomous: false
requirements:
  - SKL-01
  - SKL-02

must_haves:
  truths:
    - "All three SOS CSVs (May 19, March 17, March 10) have been processed by the qualified-candidates skill and normalizer, producing structured markdown in data/elections/"
    - "May 19 election directory contains an overview file plus statewide contest files and county contest files regenerated from the SOS CSV"
    - "March 17 and March 10 special election directories contain election files generated from their respective SOS CSVs"
    - "Candidate stub files exist in data/candidates/ with UUIDs assigned by the normalizer"
    - "Running the normalizer on each election directory reports zero structural errors and zero changes (output is already normalized)"
  artifacts:
    - path: "data/elections/2026-05-19/"
      provides: "May 19 general primary election markdown files"
    - path: "data/elections/2026-03-17/"
      provides: "March 17 special election markdown files"
    - path: "data/elections/2026-03-10/"
      provides: "March 10 special election markdown files"
    - path: "data/candidates/"
      provides: "Candidate stub files for all three elections"
  key_links:
    - from: "data/elections/2026-05-19/"
      to: "data/candidates/"
      via: "candidate stubs referenced from contest files"
      pattern: "data/candidates/"
---

<objective>
Process all three available GA SOS qualified candidates CSVs (May 19 general primary, March 17 special, March 10 special) through the qualified-candidates skill and normalizer to produce demo-ready markdown.

Purpose: Per user decision, Phase 3 produces both tools AND processed markdown for all three elections. This validates the skills work on real data and produces the demo-ready output that Phase 4 will convert and import.

Output: Complete election directories for all three elections with normalized markdown and candidate stubs.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.0-phases/03-claude-code-skills/03-CONTEXT.md
@.planning/milestones/v1.0-phases/03-claude-code-skills/03-03-SUMMARY.md
@.planning/milestones/v1.0-phases/03-claude-code-skills/03-04-SUMMARY.md

@.claude/skills/qualified-candidates/SKILL.md
@.claude/skills/normalize/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Process all three SOS CSVs and normalize output</name>
  <files>
    data/elections/2026-05-19/,
    data/elections/2026-03-17/,
    data/elections/2026-03-10/,
    data/candidates/
  </files>
  <action>
    Process each of the three SOS qualified candidates CSVs using the qualified-candidates skill with --direct flag, then run the normalizer on each output directory.

    The three CSV files are located in the data/new/ directory (worktree). Identify the exact filenames by listing the directory contents.

    For each CSV, in this order (smallest first to validate, largest last):

    1. **March 10 special election** (~38 rows):
       - Run the qualified-candidates skill on the March 10 CSV with --direct
       - Run: `uv run voter-api normalize elections data/elections/2026-03-10/`
       - Run: `uv run voter-api normalize candidates data/candidates/`
       - Verify: normalizer reports zero errors

    2. **March 17 special election** (~9 rows):
       - Run the qualified-candidates skill on the March 17 CSV with --direct
       - Run: `uv run voter-api normalize elections data/elections/2026-03-17/`
       - Run: `uv run voter-api normalize candidates data/candidates/`
       - Verify: normalizer reports zero errors
       - Compare against existing 4 files in data/elections/2026-03-17/ via git diff

    3. **May 19 general primary** (~2,346 rows):
       - Run the qualified-candidates skill on the May 19 CSV with --direct
       - Since data/elections/2026-05-19/ already has ~200 files, the skill should offer regenerate mode -- choose regenerate to validate the skill produces equivalent or better output
       - Run: `uv run voter-api normalize elections data/elections/2026-05-19/`
       - Run: `uv run voter-api normalize candidates data/candidates/`
       - Verify: normalizer reports zero errors
       - Run `git diff --stat data/elections/2026-05-19/` to compare against existing files

    After all three elections are processed:
    - Run the normalizer a second time on each directory to prove idempotency (zero changes reported)
    - Report total file counts: election files per directory, candidate stubs created
  </action>
  <verify>
    <automated>test -d data/elections/2026-05-19 && test -d data/elections/2026-03-17 && test -d data/elections/2026-03-10 && ls data/candidates/*.md 2>/dev/null | head -5 && echo "All election directories and candidate stubs exist"</automated>
  </verify>
  <done>All three SOS CSVs processed. May 19 general primary, March 17 special, and March 10 special election directories contain normalized markdown. Candidate stubs exist in data/candidates/ with UUIDs assigned. Normalizer reports zero errors and zero changes on second run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify generated election data quality</name>
  <action>
    Present the generated output for human review. Show:
    - File counts per election directory
    - Candidate stub count in data/candidates/
    - git diff --stat summary
    - Sample contest file content for spot-checking
  </action>
  <verify>Human approves the generated output quality</verify>
  <done>Human reviewer confirms all three election directories contain correctly formatted markdown with proper title case, valid URLs, and accurate contest grouping.</done>
</task>

</tasks>

<verification>
- `test -d data/elections/2026-05-19 && test -d data/elections/2026-03-17 && test -d data/elections/2026-03-10` -- all three election directories exist
- `ls data/candidates/*.md | wc -l` -- candidate stubs exist
- `uv run voter-api normalize elections data/elections/2026-05-19/ --dry-run` -- zero changes
- `uv run voter-api normalize elections data/elections/2026-03-17/ --dry-run` -- zero changes
- `uv run voter-api normalize elections data/elections/2026-03-10/ --dry-run` -- zero changes
</verification>

<success_criteria>
- All three elections processed from CSV to normalized markdown
- May 19 output compared against existing ~200 files via git diff
- March 17 and March 10 produce new election directories
- Candidate stubs in data/candidates/ have UUID-based filenames
- Normalizer idempotency proven: second run reports zero changes
- Human reviewer approves the generated output quality
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/03-claude-code-skills/03-05-SUMMARY.md`
</output>
