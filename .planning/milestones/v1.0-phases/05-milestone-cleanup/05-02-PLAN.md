---
phase: 05-milestone-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - .planning/milestones/v1.0-phases/03-claude-code-skills/03-VALIDATION.md
  - .planning/milestones/v1.0-phases/04-end-to-end-demo/04-VALIDATION.md
autonomous: true
requirements:
  - DEM-01

must_haves:
  truths:
    - "Phase 3 VALIDATION.md has nyquist_compliant: true"
    - "Phase 4 VALIDATION.md has nyquist_compliant: true"
    - "Phase 3 per-task verification statuses reflect actual test state"
    - "Phase 4 per-task verification statuses reflect actual test state"
    - "All validation sign-off checkboxes are checked"
  artifacts:
    - path: ".planning/milestones/v1.0-phases/03-claude-code-skills/03-VALIDATION.md"
      provides: "Nyquist-compliant validation for Phase 3"
      contains: "nyquist_compliant: true"
    - path: ".planning/milestones/v1.0-phases/04-end-to-end-demo/04-VALIDATION.md"
      provides: "Nyquist-compliant validation for Phase 4"
      contains: "nyquist_compliant: true"
  key_links: []
---

<objective>
Run Nyquist validation for phases 3 and 4 to close the validation gaps identified in the milestone audit.

Purpose: Phases 1 and 2 already have nyquist_compliant: true. Phases 3 and 4 are still at draft/false. Completing validation for all phases ensures the milestone has full Nyquist coverage before archival.

Output: Updated VALIDATION.md files for phases 3 and 4 with actual test results and nyquist_compliant: true.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v1.0-phases/03-claude-code-skills/03-VALIDATION.md
@.planning/milestones/v1.0-phases/04-end-to-end-demo/04-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate Phase 3 (Claude Code Skills)</name>
  <files>.planning/milestones/v1.0-phases/03-claude-code-skills/03-VALIDATION.md</files>
  <action>
Run the Phase 3 validation tests to determine actual state, then update 03-VALIDATION.md:

1. **Check Wave 0 test files exist**: Verify each file listed in Wave 0 Requirements actually exists:
   - `tests/unit/lib/test_normalizer/__init__.py`
   - `tests/unit/lib/test_normalizer/test_title_case.py`
   - `tests/unit/lib/test_normalizer/test_rules.py`
   - `tests/unit/lib/test_normalizer/test_golden_files.py`
   - `tests/unit/lib/test_normalizer/test_idempotency.py`
   - `tests/fixtures/normalizer/` directory
   Use `ls` to check. Mark existing files with [x] in Wave 0 Requirements.

2. **Run per-task automated commands**: Execute each test command listed in the Per-Task Verification Map:
   - `uv run pytest tests/unit/lib/test_normalizer/test_title_case.py -x`
   - `uv run pytest tests/unit/lib/test_normalizer/test_rules.py -x`
   - `uv run pytest tests/unit/lib/test_normalizer/test_golden_files.py -x`
   - `uv run pytest tests/unit/lib/test_normalizer/test_idempotency.py -x`
   Mark each Status as green/red/missing based on results.

3. **Update VALIDATION.md frontmatter**: Set `status: validated`, `nyquist_compliant: true`, `wave_0_complete: true` (if all Wave 0 items exist).

4. **Check all sign-off boxes**: Update each Validation Sign-Off checkbox based on actual findings.

5. **Set Approval**: Change `pending` to `approved` with date.

Commit: `test(phase-03): complete Nyquist validation`
  </action>
  <verify>
    <automated>grep "nyquist_compliant: true" .planning/milestones/v1.0-phases/03-claude-code-skills/03-VALIDATION.md && echo "PASS"</automated>
  </verify>
  <done>Phase 3 VALIDATION.md reflects actual test state, all sign-off boxes checked, nyquist_compliant: true</done>
</task>

<task type="auto">
  <name>Task 2: Validate Phase 4 (End-to-End Demo)</name>
  <files>.planning/milestones/v1.0-phases/04-end-to-end-demo/04-VALIDATION.md</files>
  <action>
Run the Phase 4 validation checks, then update 04-VALIDATION.md:

1. **Verify Wave 0 test files exist**: Check the files listed in Wave 0:
   - `tests/unit/lib/test_converter/` directory
   - `tests/integration/test_election_event_import.py`
   - `tests/e2e/test_smoke.py::TestElections`
   Use `ls` to check. These should all exist already.

2. **Run per-task automated commands**: Phase 4 tasks are primarily manual (walkthrough execution), but run the automated test commands listed in the Sampling Rate section:
   - `uv run pytest tests/unit/lib/test_converter/ -x`
   - `uv run pytest tests/integration/test_election_event_import.py -x` (if file exists)
   - `uv run ruff check . && uv run ruff format --check .`
   Mark each per-task Status based on results. For manual-only tasks (DEM-01 walkthrough execution), mark as green since the walkthrough was successfully executed in Phase 4.

3. **Update VALIDATION.md frontmatter**: Set `status: validated`, `nyquist_compliant: true`, `wave_0_complete: true`.

4. **Check all sign-off boxes**: Update each Validation Sign-Off checkbox.

5. **Set Approval**: Change `pending` to `approved` with date.

Commit: `test(phase-04): complete Nyquist validation`
  </action>
  <verify>
    <automated>grep "nyquist_compliant: true" .planning/milestones/v1.0-phases/04-end-to-end-demo/04-VALIDATION.md && echo "PASS"</automated>
  </verify>
  <done>Phase 4 VALIDATION.md reflects actual test/execution state, all sign-off boxes checked, nyquist_compliant: true</done>
</task>

</tasks>

<verification>
- `grep "nyquist_compliant: true" .planning/milestones/v1.0-phases/03-claude-code-skills/03-VALIDATION.md` — Phase 3 validated
- `grep "nyquist_compliant: true" .planning/milestones/v1.0-phases/04-end-to-end-demo/04-VALIDATION.md` — Phase 4 validated
- `uv run pytest tests/unit/lib/test_normalizer/ -x` — normalizer tests pass
- `git log --oneline -3` — shows separate commits for Phase 3 and Phase 4 validation
</verification>

<success_criteria>
1. Phase 3 VALIDATION.md has nyquist_compliant: true with actual test results
2. Phase 4 VALIDATION.md has nyquist_compliant: true with actual test/execution results
3. All four phases now have Nyquist-compliant validation
</success_criteria>

<output>
After completion, create `.planning/milestones/v1.0-phases/05-milestone-cleanup/05-02-SUMMARY.md`
</output>
